# 技术范式雷达信源说明

## 原始发现源

| 信源 | 当前角色 | 配置 | 说明 |
|---|---|---|---|
| arXiv | 论文原点、Technical Report 专项召回 | 无 Key | 除机制词查询外，额外搜索标题/摘要中明确标注的 Technical Report 或 System Report，并在排序中优先分析 |
| OpenAlex Works | 论文发现、主题、引用、作者机构 | `OPENALEX_API_KEY` | Key 免费但必需；支持日期过滤、全文搜索和结构化作者/机构信息 |
| OpenReview | 投稿、公开评审/回复 | `OPENREVIEW_VENUES` | venue id 按会议与年份维护；当前配置为 ICLR/NeurIPS 2026，正式运行前需核对 |
| 官方研究博客 RSS/Atom | 论文外的原始技术发布 | `RESEARCH_FEED_URLS` | 只放已验证的研究机构/作者官方 Feed，不抓新闻聚合站 |
| 高优先级官方研究页面 | 无 RSS 的 Technical Report、模型技术博客与研究发布 | `PRIORITY_RESEARCH_PAGES` | 内置目录覆盖海内外基础模型、World Model、机器人组织与具名实验室；页面只提高召回，发布者身份由结构化 owner/tier 单独核验；用户追加入口只允许同域抓取并降为 `verified` |

## 扎实度与人物增强源

| 信源 | 当前角色 | 配置 | 说明 |
|---|---|---|---|
| Semantic Scholar Academic Graph | 引用、影响力引用、作者 ID、主页、代表作 | `SEMANTIC_SCHOLAR_API_KEY` | 可低频无 Key 使用，但周任务建议申请 Key；标题匹配低于阈值时不会强行关联 |
| OpenReview 回复计数 | 同行讨论 | 同上 | 回复量只作证据，不把高讨论量直接等同于技术正确 |
| OpenAlex Authors | 机构、研究主题、代表作、ORCID | `OPENALEX_API_KEY` | 姓名搜索后必须用当前论文题目二次核验身份，避免同名误配 |
| ORCID | 公开研究身份、任职、教育经历、研究者 URL、公开邮箱 | 由 OpenAlex/Semantic Scholar ID 关联 | 只读取研究者主动公开的字段；可继续检查已核验个人主页中的 `mailto:`、LinkedIn 与 GitHub |
| 个人主页 / X 作者账号 | 公开简介、职业入口、研究轨迹线索 | 主页由学术实体提供；X 使用可选 `TWITTER_BEARER_TOKEN` | 只有工作标题和作者显示名可靠对齐时才合并；导师、教育和任职不做推测 |

## 机构与研究者 Watchlist

内置 Watchlist 不是“大厂白名单”，而是四种用途分离的实体目录：

- `PRIORITY_RESEARCH_PAGES`：每周主动读取的官方研究索引。
- `ESTABLISHED_RESEARCH_ORGANIZATIONS`：身份和长期前沿产出可核验的公司研究组织、独立研究机构与具名高校实验室。
- `MONITORED_RESEARCH_ORGANIZATIONS`：应持续关注、但公开 Technical Report 连续性或独立验证尚不足的模型厂商、视频/机器人公司。
- `PRIORITY_RESEARCHERS`：值得优先核验长期轨迹的人；只有完整姓名精确命中，并同时存在公开主页或 ORCID/OpenAlex 等 ID 时才生效。

组织别名采用完整字段匹配，不再使用任意子串；因此 `OpenAI competitor`、`unfair lab` 不会命中 OpenAI/FAIR。目录不包含整所大学，也不包含 `Seed`、`GLM`、`AI Lab`、`ARC Lab` 等歧义裸词。重点研究者只能带来人物轨迹的 `verified` 证据，不能让一篇普通论文直接进入报告。完整名单、分层理由和官方核验入口见 [前沿机构与研究者 Watchlist](docs/FRONTIER_RESEARCH_WATCHLIST.md)。

## 扩散与复现源

| 信源 | 当前角色 | 配置 | 说明 |
|---|---|---|---|
| Hugging Face Daily Papers | 收藏、评论、关联代码 | 无 Key | 属于未版本化站内接口，失败不会中断主流程；不能单独生成范式 |
| GitHub Search | 实现、复现、派生仓库 | `GITHUB_TOKEN` 可选 | 优先按 DOI/arXiv ID 搜索，降低同名概念误匹配；不再按 Stars 生成项目候选 |
| Hacker News / Algolia | 技术社区讨论 | 无 Key | 只搜索已经形成的范式名称，不再只抓 Show HN |
| Follow Builders | KOL 推文、播客、官方技术博客 | 无 Key（默认公共 Feed） | 推文/播客只能作为二次解读候选，需与路线关键词匹配并由综合 Agent 再审计；官方博客可以成为原点证据 |
| X 精确标题搜索 | 作者本人发布、KOL 与研究者二次解读 | `TWITTER_BEARER_TOKEN` 可选 | 作者本人发布只用于核验公开身份；非作者解读才可成为扩散信号，高关注账号会作为 KOL 线索保留 |
| Tavily 跨站公开索引 | 发现公开可索引的 X、Reddit、小红书页面 | `TAVILY_API_KEY` 可选 | 一次组合查询控制免费额度；结果只是部分网页索引，不提供平台总量，不计入准入门槛或声量分数 |
| Reddit 官方 Data API | 精确标题搜索、帖子分数、评论量与少量高赞讨论 | 获批后配置 OAuth Client ID/Secret 与描述性 User-Agent | 必须先获得 Reddit 批准；商业用途需另行获得许可。正文只供当轮综合，之后清除，只持久化链接、指标与中文分析 |

## 已降级或停用

- Product Hunt：产品发布不能证明技术范式出现，默认流水线不调用。
- GitHub Trending / Hugging Face Models & Spaces：只代表项目或模型热度，默认流水线不调用。
- 微信公众号：当前身份和噪声控制不足，默认流水线不调用。
- Google Scholar：没有稳定官方 API，不采用页面抓取。
- LinkedIn：不做自动抓取；只有作者或机构公开页明确提供链接时才收录。
- 小红书：目前没有稳定的官方公开搜索 API，不做页面抓取；Tavily 只补充搜索引擎已经公开索引的页面，不能据此判断平台总声量。

## API 风险

1. OpenAlex 当前必须配置 API Key，否则该源会明确跳过。
2. Semantic Scholar 无 Key 限流较严格；失败时仍会用 OpenAlex + ORCID 补齐最多两位关键作者。
3. OpenReview venue id 每年变化，配置错误时可能返回空结果；应每个会议周期核对一次。
4. Hugging Face Daily Papers 使用站内接口而非稳定公开版本，属于可失效的辅助源。
5. 单一发现或增强源失败会降级；但研究总编辑生成和必需邮件投递失败会让整次任务失败并回滚去重状态，避免静默丢报告。
6. 高优先级官方页面依赖站点 HTML 结构；单站解析失败会跳过，不影响其他原点源。Z.ai、Qwen 新站、腾讯 ARC、华为诺亚等动态页面可能需要专用适配；正式 Technical Report 仍会由 arXiv 专项查询形成第二条召回路径。
7. Tavily 免费额度适合发现线索，但网页索引具有不完整性和延迟；搜索相关度不是点赞、转发或评论量。
8. Reddit 官方 API 只有在 `REDDIT_API_ACCESS_APPROVED=true` 且 OAuth 配置完整时调用；未获批准时只使用 Tavily 的公开索引线索，并明确标注覆盖不足。
