# AI 技术范式雷达体检报告

> 2026-07-18 更新。首轮 GitHub Actions 已成功运行并完成邮件投递；本轮重构只进行了本地编译、模拟与单元测试，没有再次请求真实外部 API 或发送邮件。

## 当前结论

发现、分析、跨周去重、云端定时和 SMTP 投递主链路已经跑通。首轮真实交付暴露的主要问题不在运行框架，而在研究协议与写作方式：一篇工作被直接展示为一个范式，伪 GitHub 匹配被计入声量，人物增强在 Semantic Scholar 限流时整体缺失，最终报告由固定字段直接渲染，造成评分表、证据表和割裂卡片。

本轮已经把主链路调整为“机制假说 → 技术路线综合 → 人物多源核验 → 研究总编辑 Memo”：

- 抽取 Agent 必须解释 background、朴素设计思想、技术落地和路线外延，不能把论文标题改写成范式名。
- 综合 Agent 审计每条外部证据，排除论文聚合仓库和同名误匹配，并提炼二次讨论真正关注的亮点、争议与用途。
- Follow Builders 的 KOL、播客和技术博客进入二次传播候选；它们不能单独把弱机制投票成趋势。
- 人物增强在 Semantic Scholar 之外增加 OpenAlex、ORCID 与已核验个人主页，记录机构、代表作、公开链接、邮箱和完整检索轨迹。
- 最终报告由独立研究总编辑 Agent 把多篇工作合并成 2–4 条技术路线，开头生成约 500 字 Memo；评分仍用于筛选，但不展示在交付中。

## 可靠性边界

- 单一论文、博客、OpenReview、Semantic Scholar、GitHub、HN 或 Follow Builders 信源失败时继续降级运行。
- 必需邮件投递失败会让任务失败；失败任务不登记交付，并回滚本次数据库变化，修复后可完整重试。
- GitHub Actions 只在任务成功后保存去重数据库，避免云端失败状态污染下一周。
- GitHub 搜索先做代码仓库启发式过滤，再由综合 Agent 审计；宁可漏掉弱信号，也不把论文日报当复现。
- 联系方式只收录公开专业来源，不猜测邮箱，不抓取私人信息。LinkedIn 只在 ORCID 或已核验个人主页明确链接时收录。

## 验证结果

- Python 静态编译通过。
- 16 项本地单元测试通过，覆盖范式门槛、跨周签名、聚合仓库排除、人物增强降级、无 Semantic Scholar/OpenAlex 时的人物种子、编辑质量闸门、路线式报告降级、公开联系方式、JSON prompt、邮件失败和数据库回滚。
- GitHub Actions YAML 语法通过本地解析。
- 工作流已升级为 Node 24 Actions：`checkout@v6`、`setup-python@v6`、`upload-artifact@v7`；关闭 checkout 凭据持久化以消除无用的 post-job Git 清理。

下一次真实验收应重点检查报告是否真正形成少数连贯技术路线、人物背景与公开入口是否补齐、KOL/社区证据是否与技术机制实质相关，而不是只检查任务是否成功。
