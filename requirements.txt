# AI Deal Sourcing Agent - 依赖清单
# 安装: pip install -r requirements.txt

# HTTP 异步客户端
httpx>=0.27

# OpenAI 兼容 SDK（用于调用 DashScope / 火山方舟 / Ollama）
openai>=1.30

# 环境变量管理
python-dotenv>=1.0

# 定时任务调度
apscheduler>=3.10,<4.0

# API 与 LLM 调用的指数退避重试
tenacity>=8.2,<10.0
